# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g. crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
name = "stm32f429i_disco"
version = "0.1.0"
authors = ["Grégory Emery <greg.emry@gmail.com>"]
description = "Measure pin capacitance using timer capture"
keywords = ["arm", "cortex-m", "template"]
categories = ["embedded", "no-std"]
license = "MIT OR Apache-2.0"
[profile.release]
lto = true
debug = true
[dependencies.cortex-m-semihosting]
version = "0.2.0"

[dependencies.cortex-m-rt]
version = "0.4.0"

[dependencies.cortex-m]
version = "0.4.3"

[dependencies.volatile]
version = "0.2.3"

[dependencies]
stm32f429 = { git = "https://github.com/wheelin/stm32f429_rust_mmap" , features = ["rt"], version = "0.1.1"}
bitflags = "1.0"
panic-abort = "0.1.1"
bare-metal = "0.2.0"
